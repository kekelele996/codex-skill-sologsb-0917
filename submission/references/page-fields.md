# GSB 提交页现场字段（2026-09-18 只读核对）

页面：`https://solo2.jzxhnh.com/app/gsb/submit`

## 页面状态

- 当前表单共 20 个字段，页面完成度显示“必填项 0 / 19”。
- `备注` 是唯一选填字段。
- `环境可复现等级` 页面说明写了“选填”，但官方 schema 的 `is_required=true`；审核按必填处理。
- 页面提示：`填写内容会自动存为本地草稿。`
- 底部按钮：`清空重填`、`提交并质检`。审核阶段不得点击任何一个。

## 字段顺序

| 序号 | 页面字段 | 控件 | 必填 | 约束/枚举 |
|---:|---|---|---|---|
| 1 | User Prompt | 多行文本 | 是 | 完整原文，不得摘要或改写；平台逐字回验两份轨迹 |
| 2 | 任务类型 | 下拉 | 是 | 0-1代码生成 / feature迭代 / Bug修复 / 代码理解 / 代码重构 / 工程化 / 代码测试 |
| 3 | 任务难度 | 下拉 | 是 | 困难 / 地狱 |
| 4 | 语言/框架 | 单行文本 | 是 | ≤255 字 |
| 5 | Harness | 下拉 | 是 | Claude Code / Codex CLI |
| 6 | Harness 版本 | 单行文本 | 是 | ≤64 字，A/B 必须一致；本项目固定 `2.1.197` |
| 7 | 操作系统 | 下拉 | 是 | MacOS/Linux / Windows |
| 8 | 环境可复现等级 | 下拉 | 是 | 无外部依赖 / 有外部依赖，未容器化 / 已容器化，可一键起环境 |
| 9 | 初始环境快照 | URL | 是 | GitHub commit permalink，40 位完整 SHA |
| 10 | A-SessionID | 单行文本 | 是 | ≤128 字，A/B 不同 |
| 11 | A-轨迹文件 | 文件 | 是 | 1 个 `.jsonl`，≤27 MB |
| 12 | A-产物快照 | URL | 是 | 父提交必须等于初始环境快照 |
| 13 | A-运行录屏 | 文件 | 是 | 1 个 mp4/mov/webm/m4v，≤500 MB |
| 14 | B-SessionID | 单行文本 | 是 | ≤128 字 |
| 15 | B-轨迹文件 | 文件 | 是 | 1 个 `.jsonl`，≤27 MB |
| 16 | B-产物快照 | URL | 是 | 父提交必须等于初始环境快照 |
| 17 | B-运行录屏 | 文件 | 是 | 1 个 mp4/mov/webm/m4v，≤500 MB |
| 18 | GSB 结论 | 下拉 | 是 | A 更好 / Same / B 更好 |
| 19 | GSB 理由 | 多行文本 | 是 | 页面最少 60 字，A/B 过程与产物都要写清 |
| 20 | 备注 | 多行文本 | 否 | 可空 |

## 四个上传槽位

| 字段键 | 页面字段 | 接受格式 | 大小上限 | 现场状态 |
|---|---|---|---|---|
| `a_trace_file` | A-轨迹文件 | `.jsonl`，1 个 | 27 MB | 尚未上传 |
| `a_screencast` | A-运行录屏 | mp4 / mov / webm / m4v，1 个 | 500 MB | 尚未上传 |
| `b_trace_file` | B-轨迹文件 | `.jsonl`，1 个 | 27 MB | 尚未上传 |
| `b_screencast` | B-运行录屏 | mp4 / mov / webm / m4v，1 个 | 500 MB | 尚未上传 |

页面明确提示：轨迹和视频选择后“上传完成即存入对象存储，提交时不会再传一次”。本技能不使用文件选择器，统一由 Python API 在完整审核通过后上传；只有 `change-volume-line-gate` 例外需要等待 `liudong` 批准。

## 页面判定口径

- G1：本期只收困难或地狱。
- G2/G3：远端只允许主分支（main 或 master）加 A、B；两个产物快照的父提交都必须等于初始环境快照。
- G5/G6/G7：GSB 理由至少 60 字，分别覆盖 A、B 的过程与产物好坏；Same 也必须写等价点和相互抵消项。
- GSB 理由必须严格使用纯文本，不得包含任何 Markdown 语法；标题、列表、代码块、行内代码、表格、引用、链接、图片、强调标记和 HTML 标签均阻断。
- GSB 理由必须让 A、B 两侧都分别覆盖过程与产物；过程写到实际动作和定位节点，产物写到可观察结果。只写交付物毛病或只写执行动作都会被判为缺少另一层。
- 不得引用录屏、视频、截图、浏览器、测试设备、运行环境、验收宿主、Otty、鼠标、分辨率、终端窗口等场外因素；这些不能进入 GSB 理由，没有例外。
- 不得引用 `evaluationExcluded` 的环境或工具噪声证据。
- 禁用“闭环”“根因”“落库”，数据写入统一写“入库”；“真实”“真正”“其实”等空泛表达会给出警告。
- 句子必须达到高中语文阅读水平且通顺，单句非空白字符不得超过 56 字；分句过多、重复标点、标点不成对、连接词堆叠、重复虚词和残句均阻断。
- A：Prompt 与本期已有数据雷同即废弃。

- G11：发布阶段仍生成 A/B 产物快照并记录每侧改动量；最终提交预检中，任一侧少于 10 行会被阻断。只有该门禁是唯一阻断项时，才允许 `liudong` 批准 `change-volume-line-gate` 例外。
## 2026-09-19 G5 实测补充

- 负面过程评价必须有 `objectiveConsequence`，并原样写进 GSB 理由。
- 客观后果写失败本身结果，不写“后续通过”；例如“修改没有落地”“服务没有启动”。
- 负面评价至少保留一个可核对证据：文件名、函数名、命令、报错原文、退出码或接口状态。
- 不用三位以上的机器式步骤序号和纯命令粘贴；“修改服务构造函数时”“验证阶段启动服务时”更符合人工写法。
## 2026-09-20 G5 实测补充

- 平台打回：整段只写了旧唯一约束未移除、索引仍含旧约束、接口少字段和需求覆盖情况，平台判定“过程与产物未同时覆盖”。这些事实都属于产物层。
- 过程层不能只补“过程中进行了检查”；必须写某一步、某文件、某命令或某需求下实际读取、修改、验证、排查或返工了什么，以及最终造成了什么结果。
- 每侧都要有过程与产物两类事实，且草稿 claim 的 `text` 要原样进入 GSB 理由；本地 `validate_draft` 和提交预检会分别阻断。
